1) Создаем базу данных alexandrstore, если создавать через pgadmin, то можно создать стандартного юзера с дефолтным паролем - 12345;
2) Выполняем в базе код из файла script.txt;
3) В файле appsettings.json проверяем строку подключения, заменяем сервер(localhost), юзера и пароль при необходимости.